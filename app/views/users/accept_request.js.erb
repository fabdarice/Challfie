$(".friends_btn_<%= @user.id %>").closest('.one_following_block_border').addClass('hide');
$('#followers').html("<%=j render 'friends_tab', :friends => @followers, :friendship => 'followers' %>");
$("#pending_request_tab > span.badge").html("<%= @pending_request.count %>");
$("#followers_tab > span.badge").html("<%= @followers.count %>");